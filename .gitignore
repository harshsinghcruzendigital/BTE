node_modules/
dist/
source-assets/
qa-*.png
*-review.png
vite*.log
.DS_Store

# Runtime backend data (regenerated on server start) — never commit.
# Keeps session tokens, password hashes and captured leads out of the repo.
backend/data/

